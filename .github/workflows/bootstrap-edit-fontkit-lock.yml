name: Bootstrap Edit PDF font dependency lock

on:
  push:
    branches:
      - feature/edit-fontkit-intelligence-foundation

permissions:
  contents: write

jobs:
  lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/edit-fontkit-intelligence-foundation
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install exact font intelligence dependency
        run: npm install --save-exact @cantoo/fontkit@2.0.12

      - name: Remove one-shot bootstrap workflow
        run: rm .github/workflows/bootstrap-edit-fontkit-lock.yml

      - name: Commit generated package metadata
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json .github/workflows/bootstrap-edit-fontkit-lock.yml
          if git diff --cached --quiet; then
            echo "No package metadata changes were required."
            exit 0
          fi
          git commit -m "build(edit): pin maintained fontkit dependency"
          git push origin HEAD:feature/edit-fontkit-intelligence-foundation
